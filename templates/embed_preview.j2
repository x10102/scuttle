{% extends "base.j2" %}

{% block head %}
<script src="{{url_for('static', filename='js/clipboard.min.js')}}"></script>
{% endblock head %}

{% block content %}

<div class="flex w-full h-full items-center justify-center flex-col">
  <div class="bg-white/5 min-h-40 border border-slate-700 p-4 rounded-lg min-w-[40em] w-max flex flex-col items-center">
    <h1 class="text-2xl text-white font-bold text-center mb-2">Vytvořit Embed</h1>
    <iframe src="{{url_for('EmbedController.user_badge', uid=uid)}}" loading="eager" id="preview-iframe" width="100%" class="p-0"></iframe>
    <div class="flex flex-row w-full mb-6 justify-center items-center">
        <p class="inline mr-2 text-lg">Theme:</p>
        <select name="theme" id="sel-theme" class="bg-slate-900/70 rounded-lg text-white border-slate-700 border p-2 mr-6"></select>
        <p class="inline mr-2 text-lg">Typ:</p>
        <select name="type" id="sel-type" class="bg-slate-900/70 rounded-lg text-white border-slate-700 border p-2">
          <option value="translator">Překladatel</option>
          <option value="writer">Spisovatel</option>
        </select>
    </div>
    <i class="text-white/40 text-center mb-6 w-full">Pro Tip: Pro custom theme si napište Cheemsovi</i>
    <textarea id="ta-wikidot-code" rows="5" class="w-full p-1 bg-slate-900/70 border-slate-700 border outline-none mb-0 rounded-tr-md rounded-tl-md" readonly></textarea>
    <button class="w-full rounded-br-lg rounded-bl-lg py-2 text-center clip-copy bg-slate-900/70 transition-all hover:bg-slate-800 border-l border-r border-b border-slate-700 border-t-0 text-white mt-0" id="btn-copy-code" data-clipboard-target="#ta-wikidot-code">Zkopírovat kód</button>
  </div>
  <div class="bg-white/5 border border-slate-700 p-4 rounded-lg min-w-[40em] text-center flex flex-col items-center mt-10 w-min ">
    <p>Zde si můžete vybrat neuvěřitelně stylový odznak pro překladatelskou či spisovatelskou složku, který zobrazuje vaše úspěchy a aktualizuje se v reálném čase. Kód, který si zkopírujete, můžete vložit kamkoliv na Wikidot.</p>
  </div>
</div>  

<script src="{{url_for('static', filename='js/embed_preview.js')}}" data-tr-themes='{{translator_themes}}' data-uid="{{uid}}" data-wr-themes='{{writer_themes}}'></script>

{% endblock content %}
